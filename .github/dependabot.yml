version: 2
updates:
- package-ecosystem: npm
  directory: "/"
  schedule:
    interval: daily
  open-pull-requests-limit: 20
  ignore:
    # Butterfly 的設定變更不會報錯只會無聲退化,升級前需先讀 release notes 再手動升級
    - dependency-name: hexo-theme-butterfly
