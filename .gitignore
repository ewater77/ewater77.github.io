.DS_Store
Thumbs.db
db.json
*.log
node_modules/
public/
.deploy*/
_multiconfig.yml
# 本機的 images -> source/images 連結(VS Code Markdown 預覽用),
# 各機器自行建立(macOS: ln -s / Windows: junction),不進版控以免 Dropbox 跨平台同步衝突
# 開頭斜線很重要:不加的話會連 source/images/ 底下的圖一起忽略
/images
bash.exe.stackdump